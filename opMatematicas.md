SELECT UnitPrice + LineTotal
FROM Sales.SalesOrderDetail

SELECT UnitPrice - LineTotal
FROM Sales.SalesOrderDetail

SELECT UnitPrice / LineTotal
FROM Sales.SalesOrderDetail

SELECT UnitPrice * LineTotal
FROM Sales.SalesOrderDetail

Sum, Avg, min e max tambem sao operadores
Round - serve para arredondar
SQRT - raiz quadrada

Exercicio 33
Usar a funcao round em outra tabela com numeros